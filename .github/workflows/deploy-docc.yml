name: Deploy DocC

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Build DocC
        run: |
          mkdir -p .build/docc

          xcrun docc convert \
            console-log-experiment/Sources/console-log-experiment/ConsoleLogExperiment.docc \
            --fallback-display-name "console-log-experiment" \
            --fallback-bundle-identifier "com.example.console-log-experiment" \
            --fallback-default-module-kind "Command-line Tool" \
            --output-dir ".build/docc"

      - name: Prepare Pages
        run: |
          mkdir -p .build/pages
          cp -R .build/docc/. .build/pages/

          mkdir -p .build/pages/console-log-experiment
          cp -R .build/docc/. .build/pages/console-log-experiment/

      - name: Check Pages files
        run: |
          find .build/pages -maxdepth 6 -type f | sort

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ".build/pages"

  deploy:
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
